services:
  crn-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: crn-api
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:8080
    restart: unless-stopped